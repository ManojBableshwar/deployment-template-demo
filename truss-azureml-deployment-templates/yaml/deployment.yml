# AzureML managed online deployment — slim, DT-inheritance model.
# Port, path, probes, env vars, and mount are INHERITED from the deployment template
# via properties.azureml.deploymentTemplateOverride. Do NOT duplicate them here.
$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json
name: truss-vllm-dep
endpoint_name: truss-vllm-qwen35-a100
model: azureml://registries/<your-registry>/models/truss-vllm-qwen35/versions/1
instance_type: Standard_NC24ads_A100_v4
instance_count: 1
properties:
  azureml.deploymentTemplateOverride: "azureml://registries/<your-registry>/deploymenttemplates/truss-vllm-qwen35-tp1/versions/1"
