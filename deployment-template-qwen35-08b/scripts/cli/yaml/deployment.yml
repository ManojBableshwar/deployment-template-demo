# Minimal deployment YAML -- the deployment template linked to the model
# provides: environment, probes, scoring port/path, env vars, request settings.
# Only model, endpoint, and instance type are needed here.
# NOTE: env vars set here are ignored when a DT is attached -- set them in the DT instead.
$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json
name: qwen35-vllm
endpoint_name: qwen35-endpoint
model: azureml://registries/mabables-reg-feb26/models/Qwen35-08B/versions/50
instance_type: Standard_NC24ads_A100_v4
instance_count: 1
