# Dummy model registration for the "baked-in weights" approach.
# When weights are baked into the container image, AzureML still requires
# a registered model asset for the deployment template to reference.
# This is a minimal placeholder.
#
# For the "mounted weights" approach (milestone 2), this would reference
# the actual model artifacts uploaded via azcopy.
$schema: https://azuremlschemas.azureedge.net/latest/model.schema.json
name: truss-qwen35-08b
version: "1"
description: "Qwen/Qwen3.5-0.8B for Truss PoC (weights baked into container)"
path: ../truss-model/model
tags:
  framework: truss
  hf_model_id: Qwen/Qwen3.5-0.8B
  packaging: baked-in-container
